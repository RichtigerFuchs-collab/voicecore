# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Node
node_modules/
# dist/ wird eingecheckt damit Render kein Node braucht

# Lokale Env-Variablen (kein Secret, aber lokal-spezifisch)
.env.local

# Obsidian-Abholer: enthält Pfad zum Service-Account-Key – NIE einchecken!
# (auch die plattformspezifischen Varianten .win32.json / .darwin.json)
tools/obsidian_sync.config*.json
!tools/obsidian_sync.config.example.json
*service-account*.json
# Zwischengespeicherte Einträge aus fehlgeschlagenen Läufen (Notizinhalte!)
tools/.pending/

# macOS
.DS_Store
