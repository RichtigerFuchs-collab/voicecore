═══════════════════════════════════════════════════════════════
  VOICECORE V1 – FORTSCHRITT
  Stand: 26.02.2026
═══════════════════════════════════════════════════════════════

━━━ WAS WIR BAUEN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Voice-Processing-System: Sprache → Whisper → GPT → Google Docs
Phase 1 (erledigt): Mikrofon → Upload → FastAPI Backend (Cloud)

━━━ TECHNISCHER STACK ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Frontend : React + Vite → Netlify
  Backend  : FastAPI + Python 3.13 → Render.com
  Pfad     : C:\Users\Kim\OneDrive\Dokumente\Archiv\2020\Dokumente\Claude\voicecore\

━━━ LIVE URLS ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Frontend : https://voicecore.netlify.app
  Backend  : https://voicecore.onrender.com
  GitHub   : https://github.com/RichtigerFuchs-collab/voicecore

━━━ PROJEKTSTRUKTUR ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  voicecore/
  ├── render.yaml              ← Render.com Config (Repo-Wurzel!)
  ├── FORTSCHRITT.txt
  ├── README.md
  ├── .gitignore
  ├── backend/
  │   ├── main.py              ← FastAPI: /health + /upload
  │   ├── requirements.txt     ← fastapi, uvicorn, python-multipart
  │   ├── render.yaml          ← ALT, nicht mehr relevant
  │   └── .venv/               ← nicht in git!
  └── frontend/
      ├── netlify.toml         ← Netlify Build + SPA-Routing
      ├── vite.config.js       ← host:true (Windows-Fix)
      ├── .env.local           ← VITE_API_URL=http://localhost:8000
      ├── .env.production      ← VITE_API_URL=https://voicecore.onrender.com
      ├── index.html
      ├── package.json
      └── src/
          ├── App.jsx          ← komplettes UI + State Machine
          └── main.jsx

━━━ ERLEDIGTE SCHRITTE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ✓ Projektstruktur erstellt
  ✓ Frontend vollständig (React, Vite, Record-Button, State Machine)
  ✓ Backend vollständig (FastAPI /health + /upload, CORS, MIME-Check)
  ✓ Cross-Browser Audio-Support (Chrome WebM, Safari MP4, Firefox OGG)
  ✓ Python 3.13 installiert
  ✓ Python venv erstellt + requirements installiert
  ✓ npm install (node_modules vorhanden)
  ✓ vite.config.js: host:true (Fix für ERR_CONNECTION_REFUSED Windows)
  ✓ render.yaml zur Repo-Wurzel verschoben + rootDir:backend gesetzt
  ✓ netlify.toml erstellt (Build-Befehl + SPA-Redirect-Rule)
  ✓ PowerShell Execution Policy gesetzt (RemoteSigned, CurrentUser)
  ✓ LOKALER TEST ERFOLGREICH
  ✓ GitHub CLI installiert + GitHub-Account verbunden (RichtigerFuchs-collab)
  ✓ Git-Repo initialisiert + erster Commit
  ✓ GitHub Repo erstellt + Code gepusht
  ✓ Backend auf Render.com deployed → https://voicecore.onrender.com
  ✓ Frontend auf Netlify deployed → https://voicecore.netlify.app
  ✓ CLOUD-TEST ERFOLGREICH: /health gibt {"status":"ok"}, Frontend HTTP 200

━━━ PHASE 2 – Die Intelligenz (nächster Schritt) ━━━━━━━━━━━━━
  HINWEIS: Free Tier auf Render schläft nach 15 Min Inaktivität ein.
  Erster Request kann ~30 Sek dauern (cold start). Für Kathrin-Test ok.

  [ ] OpenAI Whisper API einbinden (Audio → Text)
        - OPENAI_API_KEY als Render Environment Variable setzen
        - backend/main.py: /upload ruft Whisper auf statt Stub-Response
        - pip install openai → requirements.txt ergänzen

  [ ] GPT für Post-Processing (Text → strukturierter Output)
        - Templates einbetten (Tagebuch, Quick Note, Restaurant Review)
        - GPT-Prompt je Template unterschiedlich

━━━ PHASE 3 – Die Brücke ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  [ ] Google Docs API anbinden
        - Google OAuth einrichten (Google Cloud Console)
        - google-api-python-client in requirements.txt
        - Append-Mode: Text ans Ende des jeweiligen Docs anhängen

━━━ BEFEHLE ZUM NEUSTART (lokal) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Backend (PowerShell Terminal 1):
    cd "C:\Users\Kim\OneDrive\Dokumente\Archiv\2020\Dokumente\Claude\voicecore\backend"
    .venv\Scripts\uvicorn main:app --reload --port 8000

  Frontend (PowerShell Terminal 2):
    cd "C:\Users\Kim\OneDrive\Dokumente\Archiv\2020\Dokumente\Claude\voicecore\frontend"
    npm run dev

  Browser: http://localhost:5173

  FALLS .venv fehlt (nach Clone/Umzug):
    python -m venv .venv
    .venv\Scripts\pip install -r requirements.txt

  FALLS npm Execution Policy Fehler:
    Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

  DEPLOYMENT (nach Code-Änderungen):
    cd "C:\Users\Kim\OneDrive\Dokumente\Archiv\2020\Dokumente\Claude\voicecore"
    git add .
    git commit -m "deine nachricht"
    git push
    → Render + Netlify deployen automatisch!

═══════════════════════════════════════════════════════════════
