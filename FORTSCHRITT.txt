═══════════════════════════════════════════════════════════════
  VOICECORE V1 – FORTSCHRITT
  Stand: 26.02.2026
═══════════════════════════════════════════════════════════════

━━━ WAS WIR BAUEN ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Voice-Processing-System: Sprache → Whisper/Gemini → GPT → Google Docs
Phase 1+2 (erledigt): Mikrofon → Upload → Gemini Transkription (Cloud)

━━━ TECHNISCHER STACK ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Frontend : React + Vite (gebaut, dist/ eingecheckt)
  Backend  : FastAPI + Python + Gemini 2.5 Flash API
  Hosting  : Render.com (Frontend + Backend in EINEM Service)
  Pfad     : C:\Users\Kim\OneDrive\Dokumente\Archiv\2020\Dokumente\Claude\voicecore\

━━━ LIVE URL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  App      : https://voicecore.onrender.com
  GitHub   : https://github.com/RichtigerFuchs-collab/voicecore

━━━ PROJEKTSTRUKTUR ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  voicecore/
  ├── main.py               ← Einstiegspunkt fuer Render (importiert backend.main)
  ├── render.yaml           ← Render.com Config
  ├── requirements.txt      ← Kopie von backend/requirements.txt (Render braucht das)
  ├── FORTSCHRITT.txt
  ├── README.md
  ├── .gitignore
  ├── backend/
  │   ├── __init__.py       ← macht backend zu Python-Paket
  │   ├── main.py           ← FastAPI: /health + /upload + Gemini + Static Files
  │   ├── requirements.txt
  │   └── .venv/            ← nicht in git!
  └── frontend/
      ├── dist/             ← EINGECHECKT! Muss nach Aenderungen neu gebaut werden
      ├── vite.config.js
      ├── .env.local        ← VITE_API_URL=http://localhost:8000
      ├── .env.production   ← VITE_API_URL=https://voicecore.onrender.com
      ├── package.json
      └── src/
          ├── App.jsx
          └── main.jsx

━━━ RENDER.COM EINSTELLUNGEN (Dashboard) ━━━━━━━━━━━━━━━━━━━━
  Root Directory : (leer)
  Build Command  : pip install -r requirements.txt
  Start Command  : uvicorn main:app --host 0.0.0.0 --port $PORT
  Env Variables  : GEMINI_API_KEY = [gesetzt]

━━━ ERLEDIGTE SCHRITTE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ✓ Phase 1: Frontend + Backend lokal funktionierend
  ✓ GitHub CLI installiert, Repo erstellt (RichtigerFuchs-collab/voicecore)
  ✓ Backend auf Render.com deployed
  ✓ Frontend + Backend in EINEM Render-Service zusammengeführt
  ✓ Phase 2: Gemini 2.5 Flash Transkription funktioniert
  ✓ VOLLTEST ERFOLGREICH: Aufnahme → Upload → Transkript sichtbar

━━━ OFFENE SCHRITTE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  [ ] PHASE 2b – GPT/Gemini Post-Processing
        - Templates einbetten (Tagebuch, Quick Note, Restaurant Review)
        - Gemini formatiert den transkribierten Text je nach Template
        - z.B. Tagebuch → schöner Fliesstext, Quick Note → Stichpunkte

  [ ] PHASE 3 – Google Docs API
        - Google OAuth einrichten (Google Cloud Console)
        - google-api-python-client in requirements.txt
        - Append-Mode: Text ans Ende des jeweiligen Docs anhängen

━━━ BEFEHLE (lokal) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Backend starten:
    cd "...\voicecore\backend"
    .venv\Scripts\uvicorn main:app --reload --port 8000

  Frontend starten:
    cd "...\voicecore\frontend"
    npm run dev
    → http://localhost:5173

  NACH FRONTEND-AENDERUNGEN (dist neu bauen + pushen):
    cd "...\voicecore\frontend"
    npm run build
    cd ..
    git add frontend/dist/
    git commit -m "build: frontend neu gebaut"
    git push
    → Render deployt automatisch (nur pip install, kein Node nötig)

  NACH BACKEND-AENDERUNGEN (nur pushen):
    git add .
    git commit -m "..."
    git push
    → Render deployt automatisch

  FALLS .venv fehlt:
    python -m venv .venv
    .venv\Scripts\pip install -r requirements.txt

═══════════════════════════════════════════════════════════════
